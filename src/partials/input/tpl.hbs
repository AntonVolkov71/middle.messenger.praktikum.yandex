<input
        id="{{id}}"
        class="input {{classNames}}"
        type="{{type}}"
        placeholder="{{placeHolder}}"
        value="{{value}}"
        minlength="{{minlength}}"
        required="{{required}}"
    {{#if disabled}} disabled{{/if}}
        name="{{name}}"
>

